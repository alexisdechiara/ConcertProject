<div class="grid grid-cols-3 gap-4">
    {{ form_row(participateForm.band) }}
    {{ form_row(participateForm.runningPassage) }}
    {{ form_row(participateForm.duration) }}
</div>
<div class="form-check form-switch">
    {{ form_row(participateForm.isMainBand) }}
</div>
<div class="flex space-x-2 justify-end">
    <button type="button" data-mdb-ripple="true" data-mdb-ripple-color="light" class="btn-remove inline-block px-6 py-2.5 bg-red-600 text-white font-medium text-xs leading-tight uppercase rounded shadow-md hover:bg-red-700 hover:shadow-lg focus:bg-red-700 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-red-800 active:shadow-lg transition duration-150 ease-in-out">
        Remove
    </button>
</div>
